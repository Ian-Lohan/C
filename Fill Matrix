#include <stdio.h>

int main() {
    
    int mat[10][10];
    int *ptr_mat;
    ptr_mat = mat[0];
    int i = 0;
    
    for(i=0;i<100;i++) {
        ptr_mat[i] = 0;
    }
    
    for(i=0;i<100;i++) {
        ptr_mat[i] = i+1;
        printf("%d\n",ptr_mat[i]);
    }
    
    return 0;   
}
